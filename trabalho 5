1. Estrutura de Repetição:
Um loop `for` que percorre uma lista de números e imprime cada número elevado ao quadrado.

python
# Lista de números
numeros = [1, 2, 3, 4, 5]

# Estrutura de repetição (loop for)
for numero in numeros:
# Calcula o quadrado do número
quadrado = numero ** 2
# Imprime o resultado
print(f"O quadrado de {numero} é {quadrado}")


2. Estrutura de Tomada de Decisão:
Uma condicional `if-elif-else` que verifica a faixa etária de uma pessoa e imprime uma mensagem correspondente.


# Idade da pessoa
idade = 20

# Estrutura de tomada de decisão (condicional)
if idade < 13:
print("Você é uma criança.")
elif 13 <= idade < 18:
print("Você é um adolescente.")
elif 18 <= idade < 60:
print("Você é um adulto.")
else:
print("Você é um idoso.")


Explicação:
- Estrutura de Repetição (`for` loop):O loop percorre a lista `numeros` e para cada `numero`, calcula seu quadrado e imprime o resultado.
- Estrutura de Tomada de Decisão (`if-elif-else`):A condicional verifica a idade e imprime uma mensagem dependendo da faixa etária em que a pessoa se encontra.
